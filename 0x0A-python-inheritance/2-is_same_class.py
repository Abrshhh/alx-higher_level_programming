#!/usr/bin/python3
'''Define a function.'''


def is_same_class(obj, a_class):
    '''This function specifies if an object is an instance of a class or not

    Args:
        obj: any object
        a_class: the specified class

    Returns:
        True: if the object is an instance of the class
        False: if the object is not an instance of the class
    '''

    if type(obj) = a_class:
        return True
    else:
        return False
